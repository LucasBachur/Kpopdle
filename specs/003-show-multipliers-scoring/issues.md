# Analysis Issues: Show Multipliers & Scoring Completeness

Generated by `/speckit-analyze` on 2026-06-23.

---

## HIGH

- [x] **C1 — FR-008 has no task coverage**
  - FR-008 states multipliers MUST NOT reduce any card's contribution below base stat (additive-only, non-matching cards score at 1×).
  - No task explicitly verifies or enforces this invariant.
  - **Fix**: Add a validation step to T016 Scenario S3 that confirms a non-matching card scores at exactly 1× with no penalty applied.

---

## MEDIUM

- [x] **I1 — "Logarithmic" vs exponential formula mismatch**
  - FR-017 says chemistry "scales logarithmically." T013's formula (`1 - 0.5^(n-1)`) is exponential convergence, not a logarithmic scale.
  - Observable behavior (diminishing returns) is correct; the terminology is not.
  - **Fix**: Update FR-017 wording to "diminishing returns (exponential convergence)" to match the formula, or change the formula to `log(n) / log(G_size)`. No code impact if wording is updated.

- [x] **U1 — T004 show-creation detection is underspecified**
  - T004 says "detect whether the show was newly created (check `created` flag or compare returned show `date` equality)" without pinning the exact return shape of `getOrCreateTodayShow()`.
  - **Fix**: Check `contracts/scheduler.md` to confirm the return object includes a `created: boolean` flag. If it doesn't, add a concrete detection strategy to T004.

---

## LOW

- [x] **I2 — `sizeBonus` variable undeclared in T013**
  - T013's final score line references `sizeBonus` — a variable not defined anywhere in T013 or T012. It appears to be a pre-existing local in `calculateScore()`.
  - **Fix**: Add a one-line note in T013 clarifying that `sizeBonus` is an existing local variable in `calculateScore()` so implementers don't rename or remove it.

- [x] **C2 — FR-010 post-resolution edge case not covered in T016**
  - FR-010 says "once resolved, page switches to next scheduled show's labels." T007's query handles this implicitly, but if no next show has been pre-created yet, `multiplierLabels: []` is returned instead.
  - **Fix**: Note this edge case in T016 Scenario S6 validation so it's explicitly acknowledged during manual testing.

- [x] **U2 — `ON CONFLICT DO NOTHING` target unverified for `show_multipliers`**
  - T002 uses `ON CONFLICT DO NOTHING` when inserting into the existing `show_multipliers` table, but does not confirm that table has an appropriate unique constraint.
  - **Fix**: Verify `show_multipliers` has a unique constraint on `(show_id, applies_to_type, applies_to_value)` or similar. If not, duplicates can be silently created on retry.

- [x] **D1 — Parallel "no-penalty" language in FR-008 and FR-021**
  - Both FRs use nearly identical MUST NOT reduce language — FR-008 for show multipliers, FR-021 for chemistry. Not a semantic duplicate but creates a joint maintenance obligation.
  - **Fix**: No change needed now. When modifying either formula in the future, review both FRs together. Consider testing both invariants in the same T016 pass.
